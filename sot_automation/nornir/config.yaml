inventory:
  plugin: NetBoxInventory2
  options:
    ssl_verify: true
    filter_parameters:
      status: "active"
    use_platform_slug: true

runner:
  plugin: threaded
  options:
    num_workers: 20